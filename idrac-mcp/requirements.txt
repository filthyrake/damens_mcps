# =============================================================================
# Core dependencies for working_mcp_server.py (canonical implementation)
# =============================================================================
# These are the ONLY dependencies required for the canonical JSON-RPC server
requests>=2.33.1,<3.0.0
urllib3>=2.6.3,<3.0.0
python-dotenv>=1.2.2,<2.0.0

# =============================================================================
# Optional dependencies for additional components
# =============================================================================
# The following dependencies are only needed if using optional components
# like CLI tools (fleet_cli.py, secure_fleet_cli.py, src/cli.py),
# HTTP server (src/http_server.py), or extended client features (src/idrac_client.py)
#
# To install with optional dependencies:
#   pip install -r requirements.txt
#
# To install only core dependencies for working_mcp_server.py:
#   pip install requests urllib3 python-dotenv

# HTTP clients - for async operations in src/idrac_client.py
aiohttp>=3.13.5,<4.0.0
httpx>=0.28.1,<1.0.0

# CLI frameworks - for fleet_cli.py, secure_fleet_cli.py, src/cli.py
click>=8.3.3,<9.0.0
rich>=15.0.0,<16.0.0

# Security and authentication - for src/auth.py and src/http_server.py
cryptography>=47.0.0,<48.0.0
passlib>=1.7.4,<2.0.0
python-jose>=3.5.0,<4.0.0

# Validation and logging - for src/utils/validation.py and src/utils/logging.py
# Updated to Pydantic v2 with field_validator syntax
pydantic>=2.13.3,<3.0.0
structlog>=25.5.0,<26.0.0

# Retry logic and circuit breaker
tenacity>=9.1.4,<10.0.0
pybreaker>=1.4.1,<2.0.0
