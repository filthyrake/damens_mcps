apiVersion: v1
kind: ConfigMap
metadata:
  name: proxmox-mcp-config
  namespace: proxmox-mcp
data:
  PROXMOX_PORT: "8006"
  PROXMOX_PROTOCOL: "https"
  PROXMOX_REALM: "pve"
  PROXMOX_SSL_VERIFY: "true"
  SERVER_PORT: "8000"
  SERVER_HOST: "0.0.0.0"
  LOG_LEVEL: "INFO"
  LOG_FORMAT: "json"
  MCP_USERNAME: "admin"
